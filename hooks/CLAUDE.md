# hooks

Runtime artifacts for the hook-client binary. Deployed by `make build` (copies hook-client binary here).

## Files
- hook-client: Compiled hook-client binary (deployed to this directory)
- hook_monitor.conf: Default INI config with [hooks] toggle section and [sink] forwarding config
